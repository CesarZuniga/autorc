---
name: migration
description: Create and apply a schema migration
argument-hint: <description>
---

Create a schema migration: $ARGUMENTS.

- Edit the schema, then generate the migration with the ORM's CLI.
- Review the generated SQL before applying.
- Never edit an already-applied migration; add a new one.
