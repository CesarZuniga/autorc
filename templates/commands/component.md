---
name: component
description: Scaffold a new UI component
argument-hint: <Name>
---

Create a new component named $ARGUMENTS following this project's conventions.

- Match existing file layout, naming, and styling approach.
- Type all props; no `any`.
- Include the minimal test/story if the project has that pattern.
