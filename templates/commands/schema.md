---
name: schema
description: Define or update a validation schema / type
argument-hint: <name>
---

Define or update the schema for $ARGUMENTS.

- Single source of truth; derive types from the schema, not the reverse.
- Validate at boundaries (input, env, API responses).
